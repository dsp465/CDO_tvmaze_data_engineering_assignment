resources:
  jobs:
    tvmaze_job:
      name: tvmaze_dataflow_job
      tags:
        environment: prod

variables:
  catalog: tvmaze
  schema: prod_schema
  volume_path: /Volumes/tvmaze/bronze/raw/
